import 'package:flutter/material.dart';

class RegistrationPersonalInfosStepScreen extends StatelessWidget {
  const RegistrationPersonalInfosStepScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
